---
type: Log
title: Change log
description: Dated record of updates to this bundle.
timestamp: 2026-09-19T12:00:00Z
---
- 2026-09-19: Initial bundle drafted from README.md and the code (app/, web/).
- 2026-09-19: Added image attachments (features/attachments.md); updated Todo, routes, code map, stack, trash-archive, deploy, testing.
- 2026-09-19: Documented the full-screen todo viewer (features/fields.md, code map).
- 2026-09-19: Sync/API fixes: no coalescing into sent ops, tmp ids remapped in link lists, archived link ids stay editable, X-Txn-Id validated, txn_log kept 30 days.
- 2026-09-19: UI polish from a mobile review: row menu gets scroll room above the composer on short pages (body.menu-room), todo picker clears its search after a pick, debug links larger and higher contrast, asset version bumped to v=28. No behaviour/data/API change, so no other OKF files needed.
- 2026-09-19: UI polish round 2: completed rows sink once after the last quick tap settles (1.4s); "Clear N completed" label with tooltip; diagnostics links (log, icon badge) behind a header "More" toggle; --label-3 and disabled send button contrast raised; list gets bottom room while a toast shows. Asset version v=30. No behaviour/data/API change.
- 2026-09-19: UI round 3: validation/error toast now above full-screen sheets (z-index 120); text-only actions (Cancel, Add link, Remove, Undelete, Trash, More, log, Undo) are tonal/pill buttons with 44pt targets or hit-slop; new web/sparkle.js (confetti on completion, bigger burst when everything is done, skipped under reduced motion); gradient send button, floating empty-state icon, press feedback. Asset version v=35. No behaviour/data/API change.
- 2026-09-19: APP_VERSION bumped to 35 to match asset ?v=35 (it is what makes open clients reload).
- 2026-09-19: Header shows a device-local "N done today" counter (localStorage now.doneToday, resets daily; todos carry no completion time); repeat unit labels are singular for every=1. APP_VERSION/asset v=36.
- 2026-09-19: Accent themes (Indigo/Ocean/Berry/Forest/Sunset) and an opt-in synthesized completion sound in the header More panel (web/themes.js; localStorage now.theme / now.sound). APP_VERSION/asset v=38.
- 2026-09-19: Mascot "Nudge" (web/mascot.js): peeks up from behind the add bar after 25-55s idle, blinks, says a list-aware line, ducks away; cheers when everything is done; tap to giggle; off under reduced motion; toggle in More (localStorage now.mascot). APP_VERSION/asset v=39.
- 2026-09-19: Shake to summon the mascot (accelerometer, 3 hard jolts within 0.9s, 3s cooldown); iOS needs the More panel's "Shake to summon" tap to grant motion permission (asked again after a reload); Android/desktop listen automatically. APP_VERSION/asset v=40.
- 2026-09-19: Shake detector now measures each reading's distance from gravity (>7 m/s2, three swings 100ms+ apart within 1.4s) since per-sample deltas were too small at 60Hz; More panel shows live motion strength on the Shake button; tapping the Todos title 3x also summons the mascot. APP_VERSION/asset v=41.
- 2026-09-19: Mascot is positioned from the measured composer height, above it and clipped at its top edge (z-index 35) so the add bar can never hide him; page bottom padding uses max(measured, 84px + safe-area) + 32px so Trash clears the bar; --composer-h re-measured on resize/orientation/visualViewport. Mascot voice (Sound.sfx: peek chirp, blink blip, bee-boop, boop, giggle, cheer) plays only when Sound is on; audio unlocks on first tap. Shake detector uses deviation from gravity. APP_VERSION/asset v=43.
- 2026-09-19: Mascot fully hidden when away (translate past the clip line + visibility hidden, so the antenna tip no longer shows); speech bubble is clamped inside the viewport with its tail still pointing at him (--bubble-shift). APP_VERSION/asset v=45.
- 2026-09-19: Mascot reactions (Mascot.react: 10s global gap, per-topic cooldowns, skipped when busy/hidden/up): back online, batch synced (>=3), offline, changes from another device (not at launch), first add and every 5th, done-today milestones (3/5/10/15/20/30), completing a repeating todo, welcome back after 20+ min away, once-a-day time-of-day greeting (localStorage now.greeted). APP_VERSION/asset v=46.
- 2026-09-19: sync client review fixes (delete If-Match, auth retry, 404 narrowing, undelete parent, rebuild moves, freshness retry, viewer fixes); version 47.
- 2026-09-19: Backend review fixes: move up/down maps only deliberate refusals (MoveError) to 4xx; attachment upload deletes its blob unless the stored todo lists it, orphan-blob sweep added to the daily archive run; archive moves in transactions with a re-read and rev bump, atomic last_run claim, naive timestamps read as UTC; next-up normalizes aware dues; 413 from Content-Length before reading uploads; removed legacy /parent/{id} route and dead code.
- 2026-09-19: API 404s carry explicit detail text (todo not found / attachment not found / parent not found) so the client can tell an item gone from a missing route.
- 2026-09-19: Edit sheet gets a "Pick date" chip and a tappable date field (due-time.md); app v48.
- 2026-09-19: Mascot idle pop-up is near-silent: peek, blink, beep and hide use "Soft" sine variants (gain 0.004-0.008) when started by the idle timer (`show({idle:true})`); summons (shake, triple-tap, greeting), cheers and taps keep the normal voice. APP_VERSION/asset v=49.
- 2026-09-20: Added ops/monitoring-backups.md: uptime check, 5xx and health alert policies, now_sync_conflicts metric, Firestore PITR and daily backup schedule (GCP config only, no code change).
- 2026-09-19: Edit sheet: removed the non-working "Pick date" chip; an empty due-date field now shows "Pick a date" over it (.date-field, pointer-events none). APP_VERSION/asset v=50.
- 2026-09-20: WIDGET_TOKEN moved from a plain Cloud Run env var to Secret Manager (secret widget-token); widget.md rotation steps updated. Token value unchanged.
- 2026-09-19: Archive sweep moved to a background task; claim is now a lease plus last_success (failed runs retry). Removed dead Jinja2Templates in main.py.
- 2026-09-19: Client review fixes: toast no longer wiped by apiFetch, rename keeps due/repeat, restored outbox held until first tree load + 25-day suspect-create check, back-gesture closes viewer/edit sheet, mascot tap/idle/motion fixes, attachment blob URL release, pull-to-refresh ignores row drags. APP_VERSION/asset v=51.
- 2026-09-19: Simplify pass, no behaviour change: one claimToast() owns the shared #error toast (dataset.kind marks the offline notice), first-load retry has an in-flight guard and a self-stopping timer, sync.js derives "suspect" ops at rebuild instead of flagging them on load, mascot motion listener toggling simplified, _parse_aware helper. APP_VERSION/asset v=52.
- 2026-09-19: Push reminders: app/push.py planner, /push/devices and Scheduler-only /internal/notify, sw.js + push.js, More panel regrouped (Look/Mascot/Alerts/Support), shake re-arms on first tap after relaunch, scripts/setup-push.sh; app v53. New features/push-reminders.md; routes, firestore, deploy, monitoring updated.
- 2026-09-19: Color inherited from nearest colored ancestor; Next up rows show the todo's (effective) color. app v54. features/fields.md updated.
- 2026-09-19: Push reminders confirmed on an iPhone (digest push received via Scheduler, More panel layout and shake re-arm both checked). push-reminders.md updated.
- 2026-09-19: Color bar on nested rows now bleeds to the card edge (rows use negative margin by depth) so it aligns with the parent bar; guides offset by --shift. app v55.
- 2026-09-19: Open Edit/Split/Add sheets keep typed text across re-renders (sync ack, subtask fold); previously the text was wiped and Save/Split silently did nothing. features/ordering-nesting.md updated. app v55.
- 2026-09-20: Review fixes: removed GET /todos/print and dead templates/static/Jinja2; Firestore connect + txn_log prune moved to a FastAPI lifespan (logging, not print); naive-UTC create_date; title/split length caps (422); pinned requirements + requirements-dev.txt; deploy.sh uses --no-cpu-throttling; allow-list .dockerignore, .gcloudignore, non-root image; Python tests consolidated in tests/. ops/deploy, ops/testing, data/todo, api/routes updated.
- 2026-09-20: create_date is serialized as an explicit UTC instant (Z) and the client reads bare cached values as UTC (fixes the old-outbox duplicate check being off by the UTC offset). ALLOWED_EMAIL is now required (no hardcoded fallback; set on the Cloud Run service, run.sh/README/deploy updated). app v56. data/todo, features/sync-model, architecture/stack, ops/deploy updated.
- 2026-09-20: web/app.js (2380 lines) split into ten contiguous parts (app, ui-helpers, row-interactions, render-node, editors, tree-view, composer, next-up-ui, pull-refresh, more-panel) loaded in order from index.html; concatenation is byte-identical to the old file, so behaviour is unchanged. app v57. architecture/code-map updated.
- 2026-09-20: Fork-friendliness: scripts/lib/config.sh (PROJECT/REGION/SERVICE from env, .now.env, .zilch.config), web/config.js + config.example.js (Firebase config out of auth.js), scripts/init.sh and doctor.sh, MIT LICENSE, README 'Running your own copy', Scriptable BASE placeholder. New ops/forking.md; deploy, code-map updated. app v58.
- 2026-09-20: New architecture/zilch-gcp.md (how now connects to zilch-gcp, who creates what, drift) and ops/roadmap.md (open fork/zilch items and loose ends); ops/forking.md and index linked.
- 2026-09-20: scripts/e2e.sh fixed (was 401 since the auth gate): it now serves tests_js/e2e_server.py, which overrides require_user and refuses to run without the emulator. ops/testing and ops/roadmap updated.
- 2026-09-20: init.sh --widget (creates Secret Manager secret widget-token, grants the runtime SA access, sets WIDGET_TOKEN as a secret ref); setup-push.sh timezone defaults to the machine's, not Los Angeles. ops/forking, ops/roadmap updated.
- 2026-09-20: scripts/migrate-service.sh (old service -> zilch's now-app, idempotent, --dry-run); zilch pinned Firestore PITR (a `terraform plan` showed an apply would have disabled it) and gained a 5xx alert, uptime check and optional email channel (commit 1f76d72). ops/roadmap and ops/forking updated. Migration and monitoring apply still pending.
- 2026-09-20: Prod moved to zilch's service `now-app` (scripts/migrate-service.sh; Hosting, Scheduler job, NOTIFY_AUDIENCE, IAM re-pointed; verified: health, 401 gate, Scheduler tick 200 on the new service account). Monitoring applied via zilch Terraform (uptime check, 5xx and health alerts; plan clean), hand-made duplicates deleted, now_sync_conflicts filter now targets now-app. stack, widget, monitoring-backups, zilch-gcp, forking, roadmap updated. Old service `now` left running for the widget URL.
- 2026-09-20: Old Cloud Run service `now` deleted after the widget BASE moved to https://now-app.web.app and the site was confirmed on a phone; doctor.sh clean. roadmap and zilch-gcp updated.
- 2026-09-20: Scrubbed project id/number from tracked files: .firebaserc is now git-ignored (.firebaserc.example committed); OKF and design docs use <project-id> placeholders. Git history still contains the old values (not credentials; see roadmap).
- 2026-09-20: Git history rewritten and force-pushed (main, optimistic-sync): project id/number, Firebase web config, old run.app URL and email replaced in all file contents; verified 0 hits across all blobs and an identical tip tree. Commit author email unchanged. ops/roadmap updated.
- 2026-09-20: Fix: row menu items (Edit, Add subtask, Split, ...) did nothing on a real click. `.todo-row:active` scaled the row to 0.98 while a menu item was pressed, moving the menu (a descendant) out from under the pointer so the click never fired. The press feedback now skips presses inside `.todo-menu`. No data/API change, so no other OKF files needed. app v59.
- 2026-09-20: Next up shows ALL todos due today or overdue (client sends `?today=`), filled up to 10 with the next most urgent; widget unchanged. features/next-up updated. app v60.
- 2026-09-20: Edit/Split/Add sheets are full screen width (removed the 640px cap and inline indent). app v61.
- 2026-09-20: Docs review against the code (docs only): code-map lists push.py, push.js, sw.js, auth/platform files and all scripts, and gains the client-behaviour notes moved out of push-reminders and elsewhere; ordering-nesting sheet note fixed (tree-view.js, full-screen sheets); next-up documents blocker pull-up; deploy (no stale `SERVICE=now`), forking, firestore, roadmap, routes and testing tidied. Graph rebuilt with `graphify update .`.
- 2026-09-20: Inline Edit/Split/Add sheets bleed out of the nested-list indent (margin-left = -depth*indent) so they span the card at every depth. app v62.
- 2026-09-20: Split and Add-subtask sheets are now full-screen sheets (position:fixed, edge to edge) like Edit, via renderFullSheet in editors.js. app v63.
- 2026-09-20: Voice capture: mic button records audio, `POST /todos/parse` asks Gemini (Vertex AI, service-account auth) for a draft title/due/repeat that fills the composer; `POST /todos` accepts `repeat`; `scripts/setup-voice.sh` enables the API and IAM (ran on the live project; wired into init.sh/doctor.sh). New features/voice-capture; routes, stack, code-map, forking updated. app v65.
- 2026-09-21: Calendar feed: `GET /calendar/<token>.ics` (app/ics.py) serves open dated todos as ICS behind a secret path token (`CALENDAR_TOKEN`, Secret Manager `calendar-token`); `GET /calendar/link` feeds the More panel's Subscribe / Copy link; `scripts/setup-calendar.sh` (ran on the live project), init `--calendar`, doctor check. New features/calendar-feed; routes, widget, stack, code-map, forking updated. Voice-capture mic error now names the cause. app v66.
- 2026-09-21: Fix calendar row: `web/auth.js` now attaches the token to `/calendar/link` (it was 401, so the buttons never wired up); `.more-row[hidden]` hides; Subscribe falls back to copying the link when `webcal:` is ignored. app v67.
- 2026-09-21: Removed voice capture (mic button, `POST /todos/parse`, app/voice.py, Gemini/Vertex setup, composer time/repeat chips, create-with-repeat, `google-genai`); it did not work reliably as an installed iOS app. features/voice-capture deleted; routes, stack, code-map, forking, index updated. `roles/aiplatform.user` revoked from the runtime service account (the Vertex AI API itself is left enabled, no cost). app v68.
- 2026-09-21: Mascot announces changes from another session by content (web/remote-diff.js, `refreshFromRemote`), and `Mascot.react` gained `force` to skip the polite limits for it. Replaces the generic "Fresh changes from your other device" line. app v69.
- 2026-09-21: Billing fix: always-on CPU (`--no-cpu-throttling`) plus the 10-minute reminder tick kept `now-app` billed 24/7 (about $30/month). `deploy.sh` now uses `--cpu-throttling`, live service switched, zilch main.tf pins `cpu_idle = true`; ops/deploy.md and architecture/zilch-gcp.md updated.
- 2026-09-21: Added `scripts/setup-cost-guards.sh` (Artifact Registry cleanup policy keep-3, monthly budget alert), run by `init.sh`, checked by `doctor.sh` (also fails on always-on CPU). Applied to the live project: budget `now-app monthly budget` 5 USD; other budgets already existed on the billing account.
- 2026-09-21: Reminders reduced to one 9:00 digest a day: Scheduler `now-notify` is `0 9 * * *` (was `*/10 6-23`), heads-ups and the 9:00 hour gate removed from `plan_device`, tests updated, UI text says "one morning digest". Multi-device unchanged (one digest per device per local day).
- 2026-09-21: Cost cleanup: Cloud Run max instances set to 1; deleted the dead budget-alerts Pub/Sub push subscription; disabled unused APIs (aiplatform, firebaseappdistribution, testing, runtimeconfig); fixed stale 'every 10 minutes' comments (reminder is a daily 9:00 tick). Analysis: project sits inside all free tiers; only Artifact Registry exceeded 0.5 GiB until the keep-3 cleanup policy takes effect.
- 2026-09-21: 1-hour heads-ups are back via Cloud Tasks (app/tasks.py, `POST /internal/notify-todo`): a task per timed todo, created on save and by the daily 9:00 run, re-validated when it fires; `setup-push.sh` creates queue `now-reminders` + IAM + `REMINDER_QUEUE`; `google-cloud-tasks==2.24.0` added; doctor warns when `REMINDER_QUEUE` is unset.
- 2026-09-21: Added `principles.md`: project is now a real daily-use app (no longer a learning project), single-user by design (multi-tenancy is a non-goal; forks run their own GCP project), and the #1 rule is zero GCP cost with loud reporting of any cost risk. Linked from index, stack, forking; README intro updated.
- 2026-09-21: Added `scripts/cost-check.sh` (read-only free-tier audit, loud banner, exit 1), run at the end of `deploy.sh` and by `doctor.sh`; `setup-cost-guards.sh` default budget is now $1 with a 1% threshold. First run: all free, Artifact Registry 331 MB of 512 MB (the repo's own size stat read 2.9 GB but lags; the check sums real image sizes). Open: Firestore PITR and daily backups may bill, budget alert not yet lowered on the live account.
- 2026-09-20: Heads-up scheduling on save now runs in a FastAPI BackgroundTask (after the response) and is skipped for collapse-only PATCHes.
- 2026-09-21: Budget alerts by push: `/internal/budget-alert` (Pub/Sub -> FCM, any hour, once per threshold per month), `push.budget_push/run_budget_alert`, tests, `setup-cost-guards.sh` step 3 (topic, push subscription, budget notification rule). Live budget replaced: $5 -> $1. Deployed revision now-app-00026. First post-deploy cost check FAILED loudly: 8 images = 532 MB in Artifact Registry (free tier 512 MB) because the keep-3 cleanup policy had not run yet.
- 2026-09-20: Artifact Registry cleanup policy now keeps only the newest image (was 3; single user always runs latest); deleted the 5 oldest by hand, since the policy only runs about daily and cannot be triggered.
- 2026-09-20: principles.md: rollback is an explicit non-goal (only the newest image is kept).
- 2026-09-21: Ran `setup-cost-guards.sh` live: Pub/Sub subscription `now-app-budget-push` (OIDC as now-notify, audience = NOTIFY_AUDIENCE) and the $1 budget now publishes to topic `now-app-budget-alerts`. Verified: a routine test message reached `/internal/budget-alert` and got 200 (no push sent, no threshold). A real threshold push is untested until the budget actually fires.
- 2026-09-20: Split children now get heads-ups too (POST /todos/{id}/split schedules one per new child when the split sets a due date).
- 2026-09-21: iOS review fixes (app v70): service worker caches the offline app shell (sw.js?v=, tests_js/sw.test.js); scripts `defer`; 44pt hit areas for chips/segmented; manifest id/scope/orientation/maskable icon + iPhone splash screens (generate_icons.py); meta description; `-webkit-touch-callout: none` on rows; `requirements.in` + compiled `requirements.txt` lock; vestigial `db.init(memory=)` removed. Updated code-map, push-reminders, testing, deploy, roadmap.
- 2026-09-21: UI review fixes (app v71): calm dark toast for Undo/info (red only for errors), toast no longer wraps; tinted root rows follow the card's rounded corners; subtask hairlines start at the text edge; More panel closes on outside tap/Escape; Trash hides the tabs; Split sheet shows the todo title; full-screen sheets max 640px wide; viewer detail panel is a card; picker results bordered with separators; blocked Next-up rows dimmed; bigger Synced/More chips.
- 2026-09-21: Tinted cards (app v72): the last leaf row of a colored card, at any depth, gets the card's bottom radius (children inherit the parent's color, so a colored parent with subtasks was square at the bottom).
- 2026-09-21: Composer date chips (app v73) stay in the centered column on wide screens: `.composer-chips` repeats its `auto` side margins because `.chip-row`'s `margin` was overriding them.
- 2026-09-21: New top-level todos get a random color (`POST /todos`). Python hygiene: `pyproject.toml` (ruff/mypy/pytest config), explicit imports in `app/db.py`, mypy clean, narrower lint fixes.
- 2026-09-21: `app/main.py` split into `app/routes/` routers (todos, attachments, calendar, notifications, system, common); no behaviour change.
- 2026-09-21: `app/db_firestore.py` keeps its client, todos collection, tree cache and archive timer in one `_state` object instead of module globals; no behaviour change.
- 2026-09-21: Random color for new todos moved to the client (app v74): the composer sends `color` on create and the local model applies it, since the client ignored the server-chosen color until a reload. `TodoCreate.color` is optional; the server still picks one if omitted.
- 2026-09-21: Row menu placement (app v75): the flip-up/scroll-into-view check treats a visible toast (Undo) like the composer, so the menu no longer opens under it.
- 2026-09-21: Row menu with an Undo toast (app v76): `body:has(#error:not([hidden]))` padding outranked `body.menu-room`, so the page had no extra scroll room and the menu stayed under the composer; menu-room now wins while a toast is up.
- 2026-09-21: Item types slice 1: `Todo.type` (todo/list/project), shared registry `app/types.json` (generated `web/types-data.js`), capability guards in next up, push, heads-ups, ICS, clear-completed, blocked, badge, autodone. Pickers, row and progress come in later slices.
- 2026-09-21: Item types slices 2 and 3 (app v78): row without checkbox for list/project, Make list/project/todo in the `...` menu, Type select and per-type fields in the edit sheet (hidden, not cleared), project progress over descendant todos; `Fields.isBlocked` ignores containers; registry gained `label`, `icon`, and `showsProgress` on todo.
- 2026-09-21: Item types review fixes (app v79): a container's dormant `blocked_by` no longer sets `blocked` or holds back its children in next up; `Types` ignores inherited object keys; the blocked-by picker leaves out containers.
- 2026-09-21: Stale Next up fix (app v80): API responses are `no-store` (server middleware plus `cache: "no-store"` on client fetches). Updated sync-model.
- 2026-09-21: Unified item UI (app v81): one TypePicker, ItemForm (new item and edit) and DatePicker; single "Type: X" menu entry with Undo toast; sibling-first default type; composer type chip; `type` on create and split; "subtask" wording replaced (Add item, Add several). Updated item-types, api/routes, ordering-nesting; spec `2026-09-21-unified-item-ui-design.md`.
- 2026-09-21: Unify pass (app v82): one `Toast` (web/toast.js) replaces five toast writers and their globals; sync ops are a table (`OPS`) instead of parallel switches; shared JSON fixture `tests/fixtures/tree_rules.json` run by pytest and node keeps the server and client clear-completed/blocked rules identical. No behaviour or API change. Updated code-map, sync-model, testing.
- 2026-09-21: New ops/local-browser-testing.md: how to run the app against the emulator without sign-in (scratch copy of web/, stub config.js, auth override) and drive it with the Chrome tools, plus the service-worker cache pitfall.
- 2026-09-21: Unify pass 2 (app v83): shared `web/dom.js` (one `el`, buttons, sheet action bar, label) replaces local copies in fields-ui, attachments-ui, date-picker, item-form, editors, ui-helpers; calendar-day helpers (`today`, `tomorrow`, `daysUntil`, `formatDay`, `format`) moved from ui-helpers into `due.js` with tests and an injectable `now`; item-types documents when to unify the `notifies` filter. No behaviour change.
- 2026-09-21: Trash lists most recently deleted first (by `deleted_at`, was by creation date); todos with no delete date sort after. Updated trash-archive.
- 2026-09-21: Trash fixes (app v84): the trash now lists every item, including those inside a deleted parent (they were unreachable before: a done task inside a deleted project never appeared), sorted by delete date, each row with its type icon and label; tapping a row opens it read-only (deleted date, deleted with, Undelete); an inner item can be restored on its own. API adds `deleted_with` and `trashed_at`. Updated trash-archive, api/routes.
- 2026-09-21: Trash restore, search and paging (app v85): restoring an item inside a deleted parent restores the deleted ancestor chain (the other children stay in the trash); search hits in the trash open the Trash page and that item's viewer; `GET /todos/trash` is paged (`limit` <= 100, `offset`, `has_more`) with server-side `q`, and the Trash view has Load more. `Search.matchTrashed` removed. Updated trash-archive, api/routes.
- 2026-09-21: Cost check failed after a deploy (Artifact Registry 536 MB of 512 MB free: 8 images from a day of deploys, the cleanup policy lags). Deleted the 7 old images by hand (cost check green again) and added scripts/prune-images.sh, run by deploy.sh after every deploy. Updated ops/deploy.
- 2026-09-21: Trash load 8 s -> 0.35 s: `get_trash` did one Firestore query per trashed item in sequence (N+1, ~150 ms each); it now reads the collection once and walks it in memory. Not an index problem. Updated trash-archive.
- 2026-09-21: Images in the Edit sheet (app v86): the Images section (add, view, remove) was only in the tap-a-row viewer; the Edit sheet now has it too, as registry field `attachments` on every type (viewer gates on it too). Uploads are immediate, not tied to Save. Updated attachments, item-types.
- 2026-09-21: Guard against silently losing features: CLAUDE.md rule (never remove a user-facing feature without an explicit yes; list capabilities before deleting UI code; run the browser checklist), new features/ui-inventory.md (every capability and where it lives), pre-deploy UI checklist in ops/local-browser-testing, and a test that every type keeps the images field. Images were in the viewer only; now also in the Edit sheet (v86).
- 2026-09-21: Mascot talks about the day, not the total (app v87): the once-a-day greeting and the welcome-back line now say how many are overdue and/or due today (`dueLine` in more-panel.js; "Nothing due today. Nice!" when none) instead of the open-item count. Updated ui-inventory.
- 2026-09-21: Mascot anchored to the composer by layout (app v88): he is now a child of #add-form (absolute, bottom = top of the New Item box) instead of a fixed box positioned from the measured `--composer-h`, which was stale on iOS standalone (base landed mid-box). His feet sink 16px behind the box (clipped) so wiggles never show his bottom. Updated nothing else in the inventory (no capability change).
- 2026-09-21: Mascot sinks only 6px behind the New Item box (was 16, hid his mouth) (app v89).
- 2026-09-21: Mascot has separate left/right arms; about half of pop-ups he waves with the arm away from the nearest screen edge (toward the middle; random at centre) (app v90). ui-inventory unchanged (cosmetic, no new control).
